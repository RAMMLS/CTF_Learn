# Сети и всё с ними связанное

## Инкапсуляция
**Инкапсуляция** — это включение информации предыдущего уровня в данные следующего.

---

## Модель OSI
| Уровень | Название | Описание |
|---------|----------|----------|
| 7 | Application layer | Преобразует данные в читаемый вид |
| 6 | Presentation layer | Кодирование, шифрование и т.д. |
| 5 | Session layer | Управляет сеансами связи |
| 4 | Transport layer | Определяет процесс обработки данных на конечных устройствах |
| 3 | Network layer | Реализует адресацию и маршрутизацию сети |
| 2 | Data link layer | Определяет, как информация достигает устройство |
| 1 | Physical layer | Отвечает за физическое соединение |

> 💡 Модель OSI является наглядным примером реализации инкапсуляции и не имеет практического применения.

---

## Модель TCP/IP
| Уровень | Описание |
|---------|----------|
| Application layer | Прикладной уровень |
| Transport layer | Транспортный уровень |
| Network layer | Сетевой уровень |
| Network Access layer | Уровень сетевого доступа |

> ✅ В отличие от OSI, модель TCP/IP более проста и практична.

---

## Способы передачи информации
На текущий момент самыми распространенными являются:
- 📶 Электромагнитные импульсы
- 📡 Радиоволны  
- 🔌 Оптические кабели

> На физическом уровне OSI передаются **биты**.  
> Для передачи цифровой информации по аналоговым каналам связи используется **модуляция**.

---

## Ethernet
Ethernet занимает два нижних уровня модели OSI и соответствующий уровень TCP/IP.

### MAC-адрес
**MAC (Media Access Control)** — уникальный идентификатор сетевого устройства.

**Формат:** `00:1A:2B:3C:4D:5E` (6 байт в 16-ричном виде)

**Структура:**
- **OUI** (3 байта) — идентификатор производителя
- **NIC** (3 байта) — уникальный номер устройства

**Полезные команды:**
- `macchanger` — программа для изменения MAC-адреса
- `ipconfig /all` — просмотр MAC-адреса в Windows

### Сетевые устройства
- **Коммутатор** — соединяет порт с MAC-адресом

### VLAN
**VLAN (Virtual Local Area Network)** — технология разделения одной физической сети на несколько логических подсетей.

**Access-порты** — порты, через которые конечные устройства подключаются к коммутатору и своему VLAN.

---

## Wi-Fi

### Основные характеристики
- **Диапазоны частот:** 2.4 ГГц, 5 ГГц, до 6 ГГц в современных стандартах
- **Принцип:** чем выше частота → больше пропускная способность, но меньше дальность и хуже проходимость

### Структура кадра Wi-Fi
| Поле | Размер |
|------|--------|
| Управление кадром | 2 байта |
| Длительность | 2 байта |
| Адрес-1 | 6 байт |
| Адрес-2 | 6 байт |
| Адрес-3 | 6 байт |
| Управление очередностью | 2 байта |
| Адрес-4 | 6 байт |
| Тело кадра | 0-2304 байт |
| Контрольная сумма | 4 байта |

> 🔄 Wi-Fi, как и Ethernet, разбивает данные на кадры, но структура этих кадров отличается.
